ktor {
    deployment {
        port = 8080
        port = ${?PORT}
        environment = development
        autoreload = true
        watch = [ Ramukaka ]
    }
    application {
        modules = [ com.ramukaka.ApplicationKt.module ]
    }
}
