ktor {
    deployment {
        port = 80
        port = ${?PORT}
        environment = development
        autoreload = true
        watch = [ ramukaka ]
    }
    application {
        modules = [ com.ramukaka.ApplicationKt.module ]
    }
}
