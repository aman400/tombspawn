ktor {
    deployment {
        port = 34567
        port = ${?PORT}
        environment = development
        autoreload = true
        watch = [ ramukaka ]
    }
    application {
        modules = [ com.ramukaka.ApplicationKt.module ]
    }
}
